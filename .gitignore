# Dependencies
node_modules/
# package-lock.json is tracked: `npm ci` and the CI npm cache both require it.

# Build output
out/
dist/
*.tsbuildinfo

# Logs
*.log

# Environment / secrets
.env
.env.*
!.env.example

# Local SQLite database / data (the app stores these under the OS userData path,
# but ignore any that land in the repo during development)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Editor / assistant tooling state
.claude/
CLAUDE.md
settings.local.json

# OS
.DS_Store
Thumbs.db
